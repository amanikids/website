.image
  = slideshow_tag(post)

.text
  .breadcrumbs
    = breadcrumbs(post)

  .post
    %h2= post.title
    .timestamp
      Posted on
      = post.created_at.to_date.to_s(:long_ordinal) + '.'
    = textilize post.body
